name: Deploy to Hugging Face Spaces

on:
  # push:
  #   branches:
  #     - main  
  workflow_dispatch:  

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Push to Hugging Face Spaces
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "malmir.edumail@gmail.com"
          git config --global user.name "msmalmir"
          git remote set-url origin https://username:${HF_TOKEN}@huggingface.co/spaces/msmalmir/smart-resume-optimizer
          git push origin main
